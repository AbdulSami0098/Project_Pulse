# Loaded automatically when NODE_ENV=development (set by `npm run dev`).
# Committed to git — must contain non-secret defaults only.
# Override personal values + secrets in `.env.development.local`.

PORT=3001
FRONTEND_URL=http://localhost:5173

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=project_pulse
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

REDIS_HOST=localhost
REDIS_PORT=6379

# Leave the API key unset here — put your real key in `.env.development.local`.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514
