# Loaded automatically when NODE_ENV=production (set by `npm start`).
# Committed to git — fill these in before your first production deploy.

PORT=3001

# TODO: Set to your deployed frontend domain.
FRONTEND_URL=

# TODO: Set to your production Postgres instance.
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

REDIS_HOST=
REDIS_PORT=6379

# Put the real production key in `.env.production.local` (gitignored) — never
# commit secrets to this file.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514
